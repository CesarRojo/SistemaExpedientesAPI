BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[EntrevistaInicial] ADD CONSTRAINT [EntrevistaInicial_fecha_df] DEFAULT CURRENT_TIMESTAMP FOR [fecha];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
